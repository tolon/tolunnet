;BEGIN tolunet M0 exit test
; WORK: -> boot volume (DH0:) cunku host-directory mount config'te calismadi.
; Boylece hello-task WORK:tolunet-hello.log'a yazar = HDF icine, xdftool ile okunur.
Assign WORK: DH0:
C:tolunet-hello >WORK:tolunet-stdout.log
;END tolunet
