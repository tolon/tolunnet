; tolunet M0 exit test — runs the hello-task at boot.
; Added by build automation (ci/). Runs AFTER assigns, BEFORE LoadWB.
; The hello-task writes one line to WORK:tolunet-hello.log (host-mounted dir).
If EXISTS WORK:
  C:tolunet-hello
EndIf
