# tolunnet — step RC3: close the two daemon bugs rc2 shipped with, then real release gate. One commit per item, core bench green (both profiles, `dirty: NO`) before each commit; STOP RULES unchanged; no questions — `[auto]` in QUESTIONS.md.

1. **TNET-152 (Blocker — user-facing, not bench-only).** `NetShutdown`, `TolunnetControl STOP` and `TolunnetPrefs` all stop the daemon the same way the bench does (`Signal(port->mp_SigTask, SIGBREAKF_CTRL_C)`), so if the bench cannot stop it, users cannot either. Root cause first: log in the daemon main loop which signal mask `Wait()` uses and whether `SIGBREAKF_CTRL_C` is in it after the TX pool / RECONFIG / wizard paths changed the mask; check `mp_SigTask` is the daemon task (not the CLI that created the port); check the stop path is not blocked in `WaitIO`/`AbortIO` of the TX pool. Proof: bench `tolunnet-task.log` shows **two** "lwIP 2.2.0 initialized" lines per profile, `TolunnetControl STOP` RC 0 and `STATUS` RC 5 afterwards, in a new `tc_cmd_stop_start` row.
2. **TNET-151.** Bisect inside the wizard tests, not around them: run the wizard rows one at a time with `whois` immediately after each; the first row that kills loopback names the suspect (screen open/close, `SetTaskPri`, syslog, RECONFIG apply, `ENV:` write). Fix the daemon-side cause; then restore the original test order (command rows after the wizard tail) and keep it there — the reorder workaround is not a fix. Proof: both orders green.
3. **§D.12 soak** was not run: add `ci/bench.sh soak` (a1200, 24 h; `wget` every 30 s, `ping` every 10 s, `TolunnetControl STATS` every 10 min) and run it with `TX_QUEUE=4`; pass = no Guru/freeze, RAM drift ≤ 8 KB, zero `not ok`. Record `docs/bench-logs/<dir>-soak/`.
4. **§D.13 MuForce**: if the ADF is missing under `E:\amiga\Amigatolon\tools\`, write `SKIP (tool not supplied)` in STATUS — not silence.
5. **STATUS.md drift**: still says rc1, M9 `102/142`, old SHA256s. Regenerate from the rc2 package (`make python-checks`), add M12 (rc2, `20260920-144628-50a29c8`), and state plainly that `TX_QUEUE` default is 0 in release (pool proven in bench only).
6. Then `release: 1.2.0-rc3` (lha + adf, SHA256, CHANGELOG, `docs/OWNER-RETEST.md`).

Report: item → commit → bench dir → verbatim `not ok`; two-banner proof for item 1; soak summary.
