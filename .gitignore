# Build artefacts
/build/
/obj/
*.o
*.a
*.uo
*.exe

# Amiga build outputs
*.elf
*.hunk
*.lha

# lwIP vendored from a clean release; no per-user junk inside it
vendor/lwip/**/*.o
vendor/lwip/**/*.orig
vendor/lwip/**/.git/

# Python
__pycache__/
*.pyc

# Editor / OS
*~
.*.swp
.DS_Store
Thumbs.db

# Local toolchain / env
.env
.env.*

# Intermediate dirty bench logs
docs/bench-logs/*-dirty/
docs/bench-logs/*-soakquick-*/
*-dirty/
*-soakquick-*/

# Debugger/scratch artifacts (CLOSE §A.4)
.dbg-*
.uae-*
.zoom*
.hunt*
.capture*
.wsl-*
.postkey*
.linktest*
.findsum*
.tap
.testboot/
.zcode/
_shots*/
wireless-prefs-evidence/
Claude outputs/
_to_delete/

# Soak config & local configs
ci/.soak-tolunnet.config

# AG session & agent artifacts (Step A.5 / user instruction)
TN-*.md
.agent
.ignore
AGENTS.md
graft/
/archive/
