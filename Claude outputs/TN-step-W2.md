# tolunnet — step W addendum (apply to the wizard you are building now; do not restart)

1. Page 1 "Replace other stacks": **do not import Miami/MiamiDx prefs** (binary, undocumented). Detect + disable only; the wizard collects settings fresh (DHCP default). Import stays only for Roadshow (`DEVS:Internet/interfaces`, `hosts`, `name_resolution`) and AmiTCP (`AmiTCP:db/interfaces`, `hosts`, `ENV:HostName`).
2. Detection fingerprints (add to the list): AmiTCP/Genesis — assign `AmiTCP:`, `AmiTCP:bin/startnet` or `Genesis` in `S:User-Startup`/`S:Startup-Sequence`, `AmiTCP:db/`; MiamiDx — `MiamiDx` / `Miami` / `MiamiInit` lines, `Miami:` assign, WBStartup icon; Roadshow — `AddNetInterface`/`ConfigureNetInterface` lines, `DEVS:Internet/`. Kill order for running stacks: ARexx `QUIT` on ports `MIAMI` / `MIAMIDX`, `NetShutdown` for Roadshow, `AmiTCP:bin/stopnet` for AmiTCP; wait ≤10 s; else "reboot once" message.
3. OS registration on Finish: write `ENV:HostName` + `ENVARC:HostName` (plain hostname, no newline) and `ENV:Domain`/`ENVARC:Domain` if set — many apps read these before calling `gethostname()`. Add to `docs/compat.md` under "What the OS knows about the stack": bsdsocket.library in LibList, HostName/Domain env vars, `S:User-Startup` block, optional `DEVS:Internet/*` mirror.
4. Test: `tc_wizard_wired` also asserts the fake `S:User-Startup` with Miami+AmiTCP lines is commented out with backup, and `ENV:HostName` exists after Finish.
