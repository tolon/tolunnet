# TNET-151 / TNET-152 — next diagnostic + fix. One commit per item, bench proof, STOP RULES unchanged. Do NOT re-add the parked probes yet; do item 1 first — it is a 10-line change and one bench.

## Reading of the evidence
`Wait()` returning `0x20000000` in 0 ticks with a 3 s `TR_ADDREQUEST` outstanding has two possible meanings, and the current code never distinguishes them: (a) timer.device really completed the request instantly; (b) **someone else set bit 29 on the test task** — bit 29 is `timer_port`'s bit only because it is allocated lazily (first timed call); before that, tests `AllocSignal(-1)` and free the same bit, and the daemon holds masks per base (`sig_select`, `sig_io`, `sig_event`, selectors) that it `Signal()`s from `ipc_select.c:52/97` and `slot_table.c:437` — `slot->owner_base->sig_io` is even read out of client memory. (b) is the stale-signal-bit class; (a) is essentially impossible for UNIT_MICROHZ.

1. **Distinguish (a) from (b)** — `lib_vectors.c` WaitSelect timer block and the IPC watchdog: after `Wait()`, if `(fired & tm_sig) && !CheckIO(tm)` → log `tolunnet: foreign signal on timer bit %08lx` + `tick`, count it in `GETSTATS` (`timer_foreign_wakes`), and **treat it as a spurious wake: loop back into `Wait()`** instead of reporting a timeout. Also log `tm->tr_node.io_Error` after `WaitIO`. Allocate `timer_port` **eagerly in `tn_lib_open`** (bits then stay stable for the base's lifetime). Re-add the parked probe runs after the wizard tail for this one bench. Proof: either the counter is non-zero (→ item 2) or it stays 0 and the probes still fail (→ timer.device itself; report `io_Error` and stop).
2. **If foreign:** audit every `Signal()` in the daemon for base liveness: `ipc_select.c:52` (SIGIO — copy `sig_io`/`sig_event` into the daemon at `SETSOCKETSIGNALS`/`SBTC_*` time, never dereference `owner_base` from the daemon), `ipc_select.c:97` (selector), `slot_table.c:437` (events). A slot released to the release pool (`tc_release_obtain`) or owned by a base that CLOSEd must have `owner_task = NULL` and zero masks before the CLOSE reply is sent. Host test: CLOSE → socket activity → assert no `Signal` (mock counter). Then restore command rows **after** the wizard tail and prove both orders green.
3. **TNET-152 relaunch half:** "daemon2 early death after a clean stop" — first suspect is the new duplicate-port guard: check that `RemPort(tolunnet.port)` and the SANA-II `CloseDevice` complete **before** the stop reply/exit, not after; log the guard's refusal reason at BASIC tier. Proof: two "lwIP 2.2.0 initialized" lines per profile.
4. Tree hygiene: `docs/bench-logs/20260921-025310-7707c7f/` is untracked — commit or drop; no work after a STOP-REPORT without a new step file.
