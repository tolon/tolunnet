# tolunnet — Issue & Defect Tracking Log

| ID | Severity | Milestone | Summary | Status | Resolution |
|---|---|---|---|---|---|
| **TNET-001** | Major | M1/M2 | Frame RX evidence & SANA-II hook dispatch | **RESOLVED** | SANA-II `A0/A1/D0` ASM trampolines + persistent `bm_tags` |
| **TNET-002** | Major | M2 | DHCP lease negotiation | **RESOLVED** | lwIP DHCP engine bound to SANA-II RX packet queue |
| **TNET-003** | Minor | M0 | Cross-compile portability | **RESOLVED** | Portable NDK path derived from `$(CROSS)` in Makefile |
| **TNET-004** | Minor | Architecture | Standardize project name to `tolunnet` | **RESOLVED** | Unified across all source files, headers, docs, and build targets |
| **TNET-005** | Blocker | M3 | `bsdsocket` LVO vector table offset alignment | **RESOLVED** | SFD parser + 50 vectors aligned with `bsdsocket_lib.sfd` |
| **TNET-006** | Blocker | M1 | SANA-II buffer copyfunc register calling convention | **RESOLVED** | Written in 68k assembly (`src/sana2/sana2_stubs.s`) |
| **TNET-007** | Major | M3 | `lib_OpenCnt` tracking & library expunge safety | **RESOLVED** | Atomic OpenCnt tracking under Forbid() |
| **TNET-008** | Major | M3 | `Inet_NtoA` stack corruption in RawDoFmt | **RESOLVED** | Passed 4-element ULONG array + per-opener buffer |
| **TNET-009** | Medium | M3 | Unimplemented stub return semantics | **RESOLVED** | Returns -1 with `errno = ENOSYS` |
| **TNET-010** | Minor | Docs | Protocol specification cleanup | **RESOLVED** | Documented in `docs/protocol.md` matching `include/ipc.h` |
| **TNET-011** | Minor | Logging | Clean log level defaults & S2_TRACKTYPE | **RESOLVED** | Configurable log tiers |
| **TNET-012** | Info | Architecture | Runtime dynamic library instantiation | **APPROVED** | Dynamic `MakeLibrary` / `AddLibrary` model |
| **TNET-013** | Blocker | Security | PRNG entropy seeding | **RESOLVED** | Murmur3 hash of `GetSysTime` (us) + MAC + RAM pools |
| **TNET-014** | Major | Timers | `sys_now()` precision delta time | **RESOLVED** | Computes millisecond delta from `TR_GETSYSTIME` |
| **TNET-015** | Medium | SANA-II | Asynchronous transmit queue | **RESOLVED** | Safe single-task DoIO transmit path |
| **TNET-016** | Medium | SANA-II | Zero-copy packet reception | **RESOLVED** | Direct SANA-II DMA buffer dispatch |
| **TNET-017** | Major | Build | Makefile portable NDK includes | **RESOLVED** | `NDK_INC` automatic detection |
| **TNET-018** | Minor | Debug | Assertion logging | **RESOLVED** | Debug logging via `tn_logf` |
| **TNET-019** | Medium | API | `inet_addr` bounds & octet validation | **RESOLVED** | Strict 0..255 octet, 3-dot, and trailing junk rejection |
| **TNET-020** | Minor | Perf | Flush throttling & memory attributes | **RESOLVED** | Memory buffers allocated as `MEMF_PUBLIC` |
| **TNET-021** | Blocker | Packaging | DiskObject `do_Type` offset shift in `.info` icons | **RESOLVED** | Aligned with `<workbench/workbench.h>` (`WBTOOL=3, WBPROJECT=4, WBDISK=1`) |
| **TNET-022** | Major | Packaging | Workbench data disk vs boot disk layout | **RESOLVED** | Standardized ADF as Workbench data/installation volume |
| **TNET-023** | Blocker | SANA-II | `bm_tags` stack lifetime safety | **RESOLVED** | Stored as persistent struct member in `TnSana2If` |
| **TNET-024** | Blocker | Installer | Commodore Installer LISP grammar errors | **RESOLVED** | Compliant `(welcome)` and mandatory `(help "...")` clauses |
| **TNET-025** | Medium | Tooling | `gen_lvo_table.py` authentic SFD parser | **RESOLVED** | Real parser reading `sfd/bsdsocket_lib.sfd` |
| **TNET-026** | Medium | Timers | `sys_now()` collision with in-flight periodic tick | **RESOLVED** | Dedicated `time_io` IORequest channel on `TnTimer` |
| **TNET-027** | Major | Architecture | `TolunnetStatus` linking whole lwIP stack | **RESOLVED** | Converted to thin client querying live daemon |
| **TNET-028** | Major | Compat | `sockaddr_in` zeroing & `sin_len` initialization | **RESOLVED** | Initialized `sin_len = 16` and zeroed `sin_zero` across all tools |
| **TNET-029** | Major | Correctness | `htons()` network byte order handling | **RESOLVED** | Used `htons(7)` and `htons(port)` consistently |
| **TNET-030** | Major | Diagnostic | Real ping round-trip measurement & stats | **RESOLVED** | Timed response tracking with accurate packet counts |
| **TNET-031** | Major | Memory | `TolunnetGet` RawDoFmt arg stream stack layout | **RESOLVED** | Constructed contiguous `req_args[2]` array for `path_str` & `host_str` |
| **TNET-032** | Major | Config | Configuration fragmentation across ENV blobs | **RESOLVED** | Unified on `DEVS:tolunnet.config` text KEY=VALUE format |
| **TNET-033** | Info | GUI | GadTools vs MUI preference panel | **APPROVED** | Native GadTools GUI requires 0 external dependencies (ROM 2.04+) |
| **TNET-036** | High | Compat | SocketBaseTagList return value logic | **RESOLVED** | Returns 0 on complete success; counts only unhandled tags |
| **TNET-037** | Critical | Security | TolunnetGet buffer overflow vulnerability | **RESOLVED** | Expanded `req_buf` to 1024 and capped string lengths |
| **TNET-038** | Critical | Correctness | TolunnetPing real bidirectional ping roundtrip | **RESOLVED** | Implemented `WaitSelect` timeout and `recvfrom` echo reply reading |
| **TNET-039** | High | Stability | TCP recv_cb pbuf double-free / use-after-free | **RESOLVED** | Returned `ERR_MEM` without calling `pbuf_free` on allocation failure |
| **TNET-040** | High | Correctness | tcp_write chunking and send buffer accounting | **RESOLVED** | Chunked writes to `tcp_sndbuf` and capped to 64KB per write |
| **TNET-041** | High | Correctness | WaitSelect true blocking and signal responsiveness | **RESOLVED** | Implemented polling loop with 20ms sleep slices and signal checking |
| **TNET-042** | High | Correctness | TolunnetPrefs Unit gadget state retention | **RESOLVED** | Read `LongInt` from `gad_unit->SpecialInfo` during save |
| **TNET-043** | High | Correctness | TolunnetStatus real configuration output | **RESOLVED** | Loaded active configuration from `DEVS:tolunnet.config` |
| **TNET-044** | Medium | Config | Unified text configuration format | **RESOLVED** | Standardized `DEVS:tolunnet.config` across daemon and Prefs |
| **TNET-045** | Medium | Correctness | Unit number multi-digit text formatting | **RESOLVED** | Formatted unit integer as full string in `prefs.c` |
| **TNET-046** | Medium | Correctness | SANA-II multicast vs hardware broadcast | **RESOLVED** | Restricted `is_bcast` to exact all-FF addresses |
| **TNET-047** | Medium | Security | PRNG entropy startup initialization order | **RESOLVED** | Seeded PRNG from hardware timers before `lwip_init` |
| **TNET-048** | High | Compat | Dup2Socket descriptor aliasing and lifetime leak | **RESOLVED** | Routed via `TN_IPC_CMD_DUP2` with socket slot reference counting |
| **TNET-049** | Low | Correctness | TCP error socket state mapping | **RESOLVED** | Mapped `TN_TCP_STATE_ERROR` to `ECONNRESET` / EOF on receive |
| **TNET-050** | Low | Correctness | TCP RX queue depth bounding | **RESOLVED** | Enforced `TN_MAX_RX_QUEUE_PER_SOCKET` in `tn_tcp_recv_cb` |
| **TNET-051** | Low | Compat | inet_addr dotted-quad single format constraint | **RESOLVED** | Implemented standard BSD 1-4 part decimal/octal/hex parser |
| **TNET-052** | Low | Optimization | Dead RawDoFmt cleanup in `prefs.c` | **RESOLVED** | Removed unused buffer and format call |
| **TNET-053** | Low | Tooling | Authentic SFD table generator synchronization | **RESOLVED** | Implemented `scripts/gen_lvo_table.py` parsing `bsdsocket_lib.sfd` |
| **TNET-056** | Blocker | GUI | TolunnetPrefs GadTools font pointer & library base crash (#8000000B) | **RESOLVED** | Bound real `topaz.font/8` (`FPF_ROMFONT`), universal `-m68000`, and clean GadToolsBase management |
| **TNET-057** | Major | Packaging | Installer subdrawer nesting & directory clash | **RESOLVED** | Added AmigaDOS force purge `Delete SYS:Prefs/TolunnetPrefs ALL FORCE QUIET` and `(copylib ... (infos))` |
| **TNET-058** | Major | GUI | Workbench 3.0 4-color Depth=2 icon visibility | **RESOLVED** | Standardized all `.info` icons to authentic 4-color (Depth=2, PlanePick=0x3) format with `NO_ICON_POSITION` |
| **TNET-059** | Blocker | Core | Daemon exited with `lib_OpenCnt > 0` → code segment unloaded under live per-opener clones → Guru on next client call | **FIXED (build-verified)** | `src/task/main.c`: Ctrl-C handler now refuses to exit while `g_bsd_lib->lib_OpenCnt > 0`, logs "N client(s) still have bsdsocket.library open", keeps servicing IPC, and re-checks on every Ctrl-C. No `return` from `main()` with open bases. Emulator gauntlet pending. |
| **TNET-060** | Blocker | SANA-II | `S2_CONFIGINTERFACE` failure fatal → daemon cannot restart without reboot (most drivers answer `S2ERR_BAD_STATE/S2WERR_IS_CONFIGURED` on re-config) | **FIXED (build-verified)** | `src/sana2/sana2_netif.c` `tn_s2_online`: `S2ERR_BAD_STATE + S2WERR_IS_CONFIGURED` now treated as success (logged at BASIC tier, station address re-read via `S2_GETSTATIONADDRESS` as before), mirroring the existing `S2_ONLINE`/`S2WERR_UNIT_ONLINE` tolerance. Emulator gauntlet pending. |
| **TNET-061** | Blocker | Build | `lwipopts.h`/`cc.h` tested `TOLUNET_DEBUG` but Makefile defines `TOLUNNET_DEBUG` → debug builds never enabled `LWIP_STATS`/`LWIP_PLATFORM_*`; single-n "tolunet" strings/banners across tree | **FIXED (build-verified)** | Unified on `TOLUNNET_DEBUG` (lwipopts.h:71, cc.h:50). Fixed all user-visible single-n strings (SANA-II logs, assertion log, `WORK:tolunnet-m3.log` path) and banners/guards; renamed `TnSocketBase.tolunet_port` → `tolunnet_port`. Kept `DEVS:tolunet.config` as a read-only legacy fallback path (prefs.c) deliberately. Historical docs and bench `.uae` filenames left as history. `make all DEBUG=1` now compiles with stats enabled. |
| **TNET-062** | Blocker | GUI | Prefs window 484×272 at fixed Top=25: `OpenWindow` fails on NTSC 640×200 (needs 297 px) → silent rc 0 exit; layout hard-coded | **FIXED (build-verified)** | `src/cmds/TolunnetPrefs.c` `compute_layout()`: window size/position computed from `scr->Height`, `scr->Font`, `scr->WBorTop/Bottom/Left/Right`; compact 5-row two-column form (topaz/8 ⇒ ~160 px outer height, fits 640×200 with ~20 px margin), centred on the visible area, screen font used when sane (topaz/8 ROM fallback). Emulator gauntlet (NTSC screen) pending. |
| **TNET-063** | Blocker | Config | Hostname/Secondary-DNS gadgets were cosmetic buffers; `HOSTNAME/DNS2/MTU/DEBUG` keys never parsed, written, or honoured; daemon hard-coded DNS 10.0.2.3; `DNS2=` line even overwrote the DNS1 value in the old parser | **FIXED (build-verified)** | `TnPrefs` extended (`hostname[64]`, `dns2[20]`, `mtu`, `debug`); `prefs.c` parses/writes all four keys (DNS2 no longer aliases DNS1); daemon loads prefs on every start and applies: DNS1 from config (slirp default fallback), DNS2 → `dns_setserver(1,…)`, HOSTNAME → `LWIP_NETIF_HOSTNAME` (DHCP option 12) + filled into every new opener's base for `gethostname()`, MTU clamps `netif->mtu` (576–1500, driver max respected), DEBUG sets the log tier. Grammar documented in README.guide §4 and docs/protocol.md. |
| **TNET-064** | Major | GUI/Config | Save and Use both wrote ENVARC:+DEVS:+ENV: identically; neither notified the running daemon | **FIXED (build-verified)** | `tn_prefs_save(prefs, mode)`: `TN_PREFS_USE` = ENV: session copy only; `TN_PREFS_SAVE` = ENV: + ENVARC: + DEVS:tolunnet.config (Amiga Prefs convention). Load precedence is now ENV: → DEVS: (canonical) → DEVS: legacy → ENVARC:, matching classic ENV:/ENVARC: session semantics; pre-TNET-063 blobs still load (size-tolerant reader). After Save/Use the GUI sends `TN_IPC_CMD_RECONFIG` to the running daemon (reloads and applies the live subset; interface changes logged as needing restart) or offers to start the stack via EasyRequest. |
| **TNET-065** | Major | GUI | "Start Stack" blindly `Run >NIL: C:tolunnet` → second daemon instance per click | **FIXED (build-verified)** | Button is state-aware (label follows `FindPort("tolunnet.port")`): Start runs the daemon once and waits up to ~3 s for the port to appear; Stop signals the daemon task (`port->mp_SigTask`, `SIGBREAKF_CTRL_C`) and polls until the port is gone; if clients still hold the library open (TNET-059 guard) an EasyRequest explains why the stack is still running. |
| **TNET-076** | Major | Installer | `Install_Tolunnet` static-address block passed three `(set)` forms to one `(if)` — `(if)` takes cond + then + one optional else only; strict Installer builds (CaffeineOS) reject it, so TNET-024's earlier "RESOLVED" was optimistic. Untracked working-tree leftovers `Install` (plain AmigaDOS duplicate installer) and `Install_Tolunnet-fixed` (fix draft) made the tree dirty. | **FIXED (grammar; emulator re-verify pending)** | Grammar fixes from the `-fixed` draft merged into canonical `Install_Tolunnet`: `@app-name`/`@default-dest` sets, safe defaults for the static vars, and the prompts moved into `(procedure P_ASK_STATIC)` called from a well-formed `(if …)`. Both untracked leftovers **deleted**: the Commodore Installer script is the one supported path (Installer ships with every OS 2.1+ target and is what the release archive packages); the plain AmigaDOS `Install` duplicated the flow without config integration and the `-fixed` draft is now fully merged. §3.4 audit row (config-file creation with `(exists…)` guard, uninstall path) remains open for the §3 pass. (Numbered TNET-076 so as not to collide with the v3 bugtrack's reserved TNET-066…075.) |
| **TNET-074** | Major | Docs/Build | Three contradictory CPU-target statements (README `-m68000`, Makefile header "68020+", QUESTIONS "68020+"); no opcode-scan evidence that the universal binary really avoids 68020+ instructions | **FIXED (docs + scan)** | Single truth everywhere: universal `-m68000` binary, runs 68000–68060 (README.md, QUESTIONS.md #2, STATUS.md header, Makefile header comment). Evidence: `for f in C/tolunnet C/TolunnetStatus C/TolunnetPing C/TolunnetGet C/TestSocket TolunnetPrefs; do m68k-amigaos-objdump -d $f | grep -icE "<68020+ mnemonic list>"; done` over `build/release/tolunnet` — 0 hits in all six binaries; the single raw-grep hit (`rtd` in TestSocket at 0x1fa) was verified by inspection to be the ASCII string "NtoA" inside a log-format literal, not an instruction. mnemonic list: `extb, bfins/bfext*/bftst/bfchg/bfclr/bfset, chk2, cmp2, pack, unpk, moves, muls.l, mulu.l, divs.l, divu.l, divsl, divul, linkl, rtd, pmove, FPU ops, scaled-index (*2/*4/*8) addressing`. |
| **TNET-077** | Blocker | Compat | `bind`, `listen`, `accept`, `shutdown`, `getsockname`, `getpeername` are marshaled by the library (`lib_vectors.c`) to IPC commands the daemon has **no handler for** (`src/task/main.c` `tn_handle_ipc` has no matching `case:`) → every server-style call returns `-1`/`ENOSYS`. | **RESOLVED (proven on bench)** | Implemented daemon-side IPC handlers: `TN_IPC_CMD_BIND` (ephemeral port allocation, SO_REUSEADDR support), `TN_IPC_CMD_LISTEN` (tcp_listen_with_backlog), `TN_IPC_CMD_ACCEPT` (accept queue bounded to 8, blocking/non-blocking), `TN_IPC_CMD_SHUTDOWN` (tcp_shutdown), `TN_IPC_CMD_GETSOCKNAME` / `TN_IPC_CMD_GETPEERNAME` (sockaddr_in network order fill). Proven on WinUAE bench (`docs/bench-logs/20260905-191511-3f06deb/`): `tc_bind_udp`, `tc_bind_reuse`, `tc_shutdown_wr`, `tc_getpeername` all flipped to `ok`. |
| **TNET-084** | Major | Compat | `socket()` performs **no type/protocol validation** (daemon SOCKET handler): `socket(AF_INET, 99, 0)` returns a usable-looking fd; `SOCK_RAW` is "accepted" as an fd with no raw_pcb behind it. | **RESOLVED (proven on bench)** | Implemented strict validation in `TN_IPC_CMD_SOCKET`: check domain (`AF_INET`), validate `(type, protocol)` pairs (TCP/UDP), refuse invalid types with `ESOCKTNOSUPPORT` or `EPROTONOSUPPORT`, refuse `SOCK_RAW` until §C9. Proven on WinUAE bench (`docs/bench-logs/20260905-191511-3f06deb/`): `tc_socket_types` and `tc_icmp_raw` flipped to `ok`. |
| **TNET-069** | Medium | Core | lwIP memory pool limits: insufficient TCP/UDP PCBs and segment pools for concurrent sockets | **RESOLVED (build & bench verified)** | Expanded pools in `lwipopts/lwipopts.h`: `MEMP_NUM_TCP_PCB 32`, `MEMP_NUM_TCP_PCB_LISTEN 8`, `MEMP_NUM_UDP_PCB 16`, `MEMP_NUM_RAW_PCB 8`, `MEMP_NUM_TCP_SEG 64`, `PBUF_POOL_SIZE 32`, `MEM_SIZE (96*1024)`, `SO_REUSE 1`, `LWIP_DHCP_PROVIDE_DNS_SERVERS 2`. Verified on dual-cycle bench. |
| **TNET-073** | Major | Tooling/CI | CI workflow referenced removed artifacts (`tolunet-hello`, `tolunet-amiga-build`), host-tests job ran `ls`; `tests/host` was empty — the project had zero automated tests. | **PARTIAL (L1 host; CI run pending)** | Round 3 §B: host harness built — `tests/host/tn_test.h` (TAP), pure units extracted (`inet_parse`, `config_text` + shared `tn_prefs_default`, `sbtc_dispatch`, `fdset_util`), 6 test binaries (34 cases incl. 2 intentional SKIPs and 1 TODO-marked TNET-078 red) run under `make test-host` with `-Werror -fsanitize=address,undefined`: **6 binaries, 0 failed** (L1). LVO-vs-SFD cross-check now asserted natively in C (`test_lvo_table.c`: SFD = 139 slots −30…−858; count question recorded in QUESTIONS.md #3). Amiga-side `SocketConformance` (17 TAP cases) built by `make all` and proven on the WinUAE bench (L3 baseline: `docs/bench-logs/2026*/`). `.github/workflows/build.yml` rewritten (host job runs `make test-host`, amiga job uploads `tolunnet-lha`/`tolunnet-release`) — **first GitHub Actions run still pending push**; `__pycache__` git-ignored and untracked. |
| **TNET-085** | Blocker | SANA-II/lwIP | **68000 Address Error (#80000003)** in the daemon on the WinUAE A600/68000 bench: daemon dies on first network frame due to misaligned IP header and memory pool accesses on 68000 (which forbids word/long access at odd addresses). | **RESOLVED (proven on 68000 bench)** | Fixed via: (1) `include/arch/cc.h`: `LWIP_DECLARE_MEMORY_ALIGNED(v, s)` 4-byte alignment and packed field macros; (2) `lwipopts/lwipopts.h`: `ETH_PAD_SIZE 2` (IP header lands on 4-byte boundary); (3) `src/sana2/sana2_netif.c`: frame offset padding in `tn_sana2_poll_input` (`pbuf_add_header`) and `tn_sana2_linkoutput` (`pbuf_remove_header`); (4) `Makefile`: clean removes `$(BUILD)` completely. Proven on headless WinUAE A600 (68000 ECS, 2MB Chip + 4MB Bogo) bench: two consecutive start/stop cycles, zero Gurus, successful DHCP lease `10.0.2.15` (`docs/bench-logs/20260905-183850-3082b59/68000/conformance.log`). |
| **TNET-070** | Major | Correctness | ICMP raw sockets & TolunnetPing | **RESOLVED (proven on bench)** | Implemented `SOCK_RAW` with `IPPROTO_ICMP` in daemon, `pbuf_clone` to preserve packet header while letting lwIP process echo replies, integer-math `TolunnetPing` with microsecond RTT timing, standard deviation `mdev` integer square root, UDP fallback switch (`UDP/S`). Flipped `tc_icmp_raw` to ok. |
| **TNET-071** | Major | Network | Real loopback `lo0` & netstat per-socket rows | **RESOLVED (proven on bench)** | Enabled `LWIP_NETIF_LOOPBACK 1`, `LWIP_HAVE_LOOPIF 1`, `LWIP_NETIF_LOOPBACK_MULTITHREADING 0`, polled loop netif in main task loop via `netif_poll_all()`; added `TN_IPC_CMD_ENUMSOCKETS` with `TnSocketInfo` struct; updated `netstat` to query live active sockets and print real rows. Flipped `tc_listen_accept_loopback` to ok. |
| **TNET-067** | Major | Correctness | SIGIO delivery & WaitSelect timer precision | **RESOLVED (proven on bench)** | Implemented daemon-side `Signal(owner_task, sig_io)` delivery across all socket events (RX, peer close, accept, connect complete, error); reworked `WaitSelect` to use per-opener `timer.device` IORequest with Exec `Wait()`, immediate zero-timeout poll, and 20ms poll fallback only when `sig_io == 0`. Proven on WinUAE bench (`docs/bench-logs/20260906-001249-0793186/`): `tc_sigio` and `tc_waitselect_timeout` flipped to `ok` on both A1200 and 68000. |
| **TNET-075** | Major | Commands | `TolunnetGet` (wget/curl) lacked HTTP/1.1 compliance, 301/302 redirect following, chunked transfer decoding, progress display, and AmigaOS standard `ReadArgs` | **RESOLVED (L1 host + L0 cross-build)** | Extracted pure HTTP URL parser, header parser, redirect resolver, and chunked transfer state machine into `src/common/http_url.c`; covered with 9 host unit tests under ASan/UBSan (`tests/host/test_http.c`); updated `src/cmds/TolunnetGet.c` to use `ReadArgs("URL/A,PORT/N,PATH,TO/K,QUIET/S")`, support HTTP/1.1 `Host:` and `Connection: close`, follow up to 5 HTTP 301/302/303/307/308 redirects, reject `https://` with TLS requirement warning, support Range-based resume on destination file, and display integer-based percentage progress. |

