# tolunnet — Bugtrack 2 (state: HEAD 002745, 29 OPEN rows, owner machine still Gurus). One commit per numbered item, bench proof per commit, ISSUES row updated with rung + log dir. STOP RULES: bench red twice in a row or 3 h without commit → STOP-REPORT.md. Never cite a `-dirty` log. No questions — decide by Roadshow SDK 1.8 `doc/bsdsocket.doc` + `netinclude/`, log `[auto]` lines in QUESTIONS.md.

Order is by blast radius. Do not start item N+1 before item N is committed and green.

1. **TNET-139 (REOPENED) — owner's A500+PiStorm+WiFiPi still Gurus `#80000003` at `C:tolunnet` start.** Bench cannot reproduce; the fix must be proven on the owner's machine. Do: (a) ship a `tolunnet DIAG` mode (config `DIAG=YES`): install a task-local exception trap (`tc_TrapCode`) for vectors 2–4 that writes PC, SR, fault address, function-code and the last 16 `tn_logf` lines to `RAM:tolunnet-crash.log` **before** the Guru, plus `SegTracker`-style hunk base logging at start so PC maps to `objdump -d -l`; (b) log every step of startup (config load, timer open, SANA-II `OpenDevice`/`S2_DEVICEQUERY`/`S2_GETSTATIONADDRESS`/`S2_CONFIGINTERFACE`/`S2_ONLINE`, lwIP init, `MakeLibrary`, port add) with `Flush` after each line to `LOG=`; (c) add a bench profile `pistorm-68000` that runs the WiFiPi-style driver path with **68000 CPU + Kickstart 3.2 + no Fast RAM** (the owner's `docs/PISTORM-INSTALL.md` config) — verify with the owner which emulator/CPU mode the PiStorm runs (Emu68 vs Musashi; 68000/020/040) and mirror it; (d) audit every `struct` shared with the driver (`IOSana2Req`, buffer-management tags, `S2_ONEVENT` request, `SANA2_IOF_RAW` flags) and every `AllocVec` buffer handed to the driver for `MEMF_PUBLIC` + even size; audit `wifipi.device`-specific paths (`S2_GETNETWORKS` tag lists, `sana2wireless.h` structs) for odd offsets. Deliver: new `.lha` + `docs/OWNER-RETEST.md` (3 commands, where the crash log lands). Owner runs it and sends `RAM:tolunnet-crash.log`; you fix from the PC. Until that log exists, the row stays REOPENED — no "fixed" claim.
2. **TNET-115 — bsdsocktest #32 multi-iovec freeze (6/8 runs on 68000).** Root cause first: `il 8`/`fi` in WinUAE at the freeze, `daemon.log` last lines, `tolunnet STATS` loop counter. Check in `ipc_msg.c`: iovecs read only before `ReplyMsg`; `iov_base` copied byte-wise (`CopyMem` is fine, `*(ULONG*)` is not); total length bounded to `TCP_SND_BUF`/`0xFFFF`; partial-send path returns and does not spin when `tcp_sndbuf()==0`; recvmsg with more iovecs than data terminates. Host test `test_queues`: 3-iovec gather with odd `iov_base`, zero-length iovec, iovlen>data. Proof: #32 `ok` on both profiles ×2 and no freeze in 4 consecutive full runs.
3. **Errno/h_errno pointer group — TNET-119, 120, 132, 133, 134, 135 (bsdsocktest #76, #77, #122–125).** Semantics from `bsdsocket.doc`: `SetErrnoPtr(ptr,size)` size ∈ {1,2,4}; every error path writes through the pointer with that width; `SBTC_ERRNOLONGPTR`/`HERRNOLONGPTR` **get** must return the current pointer (`SBTM_GETREF`), not fail; `SBTC_ERRNOBYTEPTR/WORDPTR` likewise. Library-side only (`lib_vectors.c`, `sbtc_dispatch.c`); host `test_sbtc` matrix extended.
4. **Descriptor table group — TNET-121, 136, 137, 138, 130, 131 (#78, #127, #128, #131, #115, #117).** `getdtablesize()` default = Roadshow's 64 (verify in doc; ours is 32 → raise `TN_MAX_FDS_PER_TASK` to a config key, default 64, `fd_map` allocated per base); `SBTC_DTABLESIZE` set grows the table live (realloc + copy, never shrink below highest open fd → `EINVAL`); `socket()` must succeed for `dtablesize-1` descriptors; `Dup2Socket(fd,-1)` = lowest free fd; dup'd fd fully usable (shares slot refcount).
5. **Event group — TNET-122…128 (#79, #81, #83–87) + TNET-117/118 (#64, #66).** `SO_EVENTMASK` per socket (setsockopt at `SOL_SOCKET`), events raised only for masked types, `FD_READ` on data, `FD_CLOSE` on peer FIN/RST, no events on idle sockets; `GetSocketEvents(&mask)` returns the fd and clears that socket's pending mask (round-robin over sockets, `-1` when none); `SBTC_SIGEVENTMASK` signal set once per new event; `WaitSelect` `exceptfds` set on OOB, `EINTR` + signal left set when an Amiga signal in `signals` mask arrives; #87 50-iteration stress must not leak events or selectors (`GETSTATS` selector count back to 0).
6. **OOB + peer-close — TNET-114, 116 (#27, #35).** `recv(MSG_OOB)` returns the urgent byte (lwIP has no OOB: keep a 1-byte urgent slot per socket fed from `tcp` URG flag via `LWIP_TCP_PCB_NUM_EXT_ARGS`/custom `tcp_input` hook is not available — implement by parsing the URG pointer in a `LWIP_HOOK_TCP_INPACKET_PCB` hook; if impossible in ≤2 h, return `EINVAL` and document WONTFIX with the reason); `send()` after peer close → `EPIPE`/`ECONNRESET` immediately, not silent success.
7. **TNET-129 getnetbyname (#101)** over `DEVS:Internet/networks` + built-in defaults (D1 database work already stashed as `wip-netdb-d1`: pop, finish only `networks`, leave the rest).
8. **TNET-140** — remove the `tn_logf` inside `Forbid()` in `lib_init.c:50-54` (log after `Permit()`); delete the excuse from `check-forbid-known.txt`.
9. **TNET-113** — `tc_multicast_join` a1200 flake: apply the fallback already written in TN-step-B item 3 (join + `S2_ADDMULTICASTADDRESS` counter + leave) and close.
10. **TNET-106/107 (perf)** — measure first with `TcpPerf` (host sink on lo0 is not valid; use slirp 10.0.2.2), then `SendIO` TX pipelining (depth = config `TX_QUEUE=`, default 4, completion port in the main `Wait()`), RX `TnRxPacket` freelist; report KB/s before/after in `docs/bench.md`.

After 2–7: bsdsocktest target ≥ 135/142 on both profiles; remaining failures each have a row with reason. Final: `make package` (lha+adf, SHA256 in STATUS), ISSUES/STATUS/README counts regenerated by `make python-checks`. Report: per item commit hash, bench dir, before/after bsdsocktest score, verbatim `not ok` lines.
