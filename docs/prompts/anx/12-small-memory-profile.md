# ANX-12 — küçük bellek profili (68000 / 2 MB)
Kurallar (değişmez): (1) yalnız bu dosyadaki iş; başka dosyaya, başka TNET'e dokunma. (2) Tek commit; iş bitmeden ara commit yok, iş bittikten sonra bekleyen değişiklik yok. (3) Kanıt = `ci/bench.sh` log dizini + host test çıktısı; kendi ifaden kanıt değildir. (4) DUR kuralı: bench üst üste 2 kez kırmızı → dur; 4 saat içinde commit yoksa → dur; durunca `STOP-REPORT.md` yaz (ne yapıldı, ne kırmızı, son yeşil commit) ve bitir. (5) Yeşil kalması gereken taban: mevcut core testleri iki profilde (a1200, 68000) — biri düşerse bu iş kırmızıdır. 
1. `lwipopts/lwipopts-small.h`: `MEM_SIZE 32K`, `PBUF_POOL_SIZE 12`, `TCP_WND 4*TCP_MSS`, `TCP_SND_BUF 4*TCP_MSS`, `MEMP_NUM_TCP_PCB 8`, `MEMP_NUM_TCP_PCB_LISTEN 2`, `MEMP_NUM_UDP_PCB 6`, `LWIP_STATS 0`, `LWIP_IPV6 0`, mDNS kapalı. Sayıları `opt.h` bağımlılık kurallarına göre doğrula (TCP_SND_QUEUELEN vs.).
2. Tek binary kalır: iki lwIP nesne seti derlenir (`build/lwip-std/`, `build/lwip-small/`) ve daemon'a iki ayrı `tolunnet` **değil** — lwIP `NO_SYS` yapılandırması derleme zamanıdır; bu yüzden **iki daemon binary'si**: `C:tolunnet` ve `C:tolunnet-small`. Installer ve `TolunnetPrefs Start`, `AvailMem(MEMF_ANY)` < `SMALL_THRESHOLD` (config, varsayılan 1.5 MB) ya da `MEMORY=SMALL` anahtarı ise `-small`'ı başlatır. Kütüphane ABI'si aynı.
3. `tolunnet STATUS` profil adını basar; `netstat -h` (ANX-04) `pbuf_min_ever` ile profilin yetip yetmediğini gösterir.
4. Bench: `ci/tolunnet-68000.uae` zaten A600 2MB — bu profilde `-small` ile tam core (34/34) + `tc_http_1mb` indirme (slirp), süre kaydı. a1200 std profil değişmez.
5. `README.md` "Key Features" satırı: "İki bellek profili; 2 MB A600'de çalışır" + ölçülen boş bellek farkı (bench'ten, elle sayı yazma).
Kabul: 68000 profilinde `-small` 34/34, commit `build: small-memory lwIP profile and runtime selection`.
