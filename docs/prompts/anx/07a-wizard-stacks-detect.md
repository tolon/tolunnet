# ANX-07a — wizard: Roadshow/Genesis dahil tespit → bilgilendir (yalnız tespit + ekran)
Kurallar (değişmez): (1) yalnız bu dosyadaki iş; başka dosyaya, başka TNET'e dokunma. (2) Tek commit; iş bitmeden ara commit yok, iş bittikten sonra bekleyen değişiklik yok. (3) Kanıt = `ci/bench.sh` log dizini + host test çıktısı; kendi ifaden kanıt değildir. (4) DUR kuralı: bench üst üste 2 kez kırmızı → dur; 4 saat içinde commit yoksa → dur; durunca `STOP-REPORT.md` yaz (ne yapıldı, ne kırmızı, son yeşil commit) ve bitir. (5) Yeşil kalması gereken taban: mevcut core testleri iki profilde (a1200, 68000) — biri düşerse bu iş kırmızıdır. Asla dosya silme; asla `AmiTCP:` assign'ına dokunma.
1. `src/setup/stack_detect.c` tespit tablosu (veri tablosu, kod değil): `AmiTCP` (`Lock("AmiTCP:")`, `AmiTCP:bin/AmiTCP`, port `AmiTCP`), `Miami` (`Miami:`/`MiamiDx:` Lock, `LIBS:miami.library`, port `MIAMI`/`MIAMIDX`), `Genesis` (`Genesis:` Lock, port `GENESIS`, `SYS:WBStartup/Genesis(.info)`), `Roadshow` (`C:AddNetInterface`, `C:NetShutdown`, `DEVS:NetInterfaces/` dizini, `LIBS:usergroup.library` + `DEVS:Internet/`), `Diğer` (`FindName(&SysBase->LibList,"bsdsocket.library")` var ama eşleşme yok → `lib_Node.ln_Name`/`lib_IdString` göster). Tüm dosya erişimleri `pr_WindowPtr=(APTR)-1` altında (`Lock`, `Open`; requester yok — TNET-112 guard kalır).
2. Startup taraması: `S:User-Startup` ve `S:Startup-Sequence`'ta her stack için bilinen satır kalıpları (`AmiTCP:bin/startnet`, `Miami:MiamiDx`, `AddNetInterface`, `Genesis:`); ayrıca `SYS:WBStartup/` içinde ilgili `.info`.
3. Sayfa "Stacks" ListView (mevcut W4 P2): satır = `<ad> <sürüm/yol> [çalışıyor|kurulu]`. Roadshow için metin: "Bu makinede Roadshow kurulu (ticari). Kullanmak istiyorsanız tolunnet'e gerek yok; istemiyorsanız devre dışı bırakabiliriz, dosyalarına dokunulmaz." Seçenekler radyo: **Devre dışı bırak** / **Olduğu gibi bırak (tolunnet'i başlatma ekle, çakışırsa uyar)** / **İptal**.
Devre dışı bırakma/geri alma bu prompt'ta YOK; radyo seçimi kaydedilir, uygulanmaz. Bench `tc_wizard_stacks_detect`: sahte `S:User-Startup` (AmiTCP+Roadshow satırları) + `DEVS:NetInterfaces/eth0` → liste 2 satır, requester yok.
Kabul: iki profilde bench yeşil (+1), commit `setup: detect AmiTCP/Miami/Genesis/Roadshow with informative page`.
