# ANX-14 — `mbuf_*` vektörleri (-624…-684) lwIP `pbuf` üstünde
Kurallar (değişmez): (1) yalnız bu dosyadaki iş; başka dosyaya, başka TNET'e dokunma. (2) Tek commit; iş bitmeden ara commit yok, iş bittikten sonra bekleyen değişiklik yok. (3) Kanıt = `ci/bench.sh` log dizini + host test çıktısı; kendi ifaden kanıt değildir. (4) DUR kuralı: bench üst üste 2 kez kırmızı → dur; 4 saat içinde commit yoksa → dur; durunca `STOP-REPORT.md` yaz (ne yapıldı, ne kırmızı, son yeşil commit) ve bitir. (5) Yeşil kalması gereken taban: mevcut core testleri iki profilde (a1200, 68000) — biri düşerse bu iş kırmızıdır. Referans: AmiNetXDuo `src/mbuf/{mbuf_ops,mbuf_packet,mbuf_alloc,mbuf_abi_check}.c` (NetX Duo paket emülasyonu — mantığı al, yapıyı değil), Roadshow SDK `netinclude/sys/mbuf.h` (yapı düzeni, `M_PKTHDR`, `MLEN`, `MHLEN`; değerleri dosyadan al).
1. `src/lib/mbuf_compat.c`: `struct mbuf` Roadshow düzeninde **gerçek bellek** (uygulama `m_data/m_len/m_next`'i doğrudan okur; opak olamaz). Her `mbuf` bir `pbuf` sarar (`m_data` → `p->payload`, `m_len` → `p->len`, `m_next` zinciri `p->next`); `pbuf_ref/pbuf_free` ile ömür; küme `PBUF_RAM`. `mbuf_get/gethdr` boş `PBUF_POOL`den; `mbuf_free/freem` pbuf'ı bırakır; `copym` `pbuf_clone`, `copydata` `pbuf_copy_partial`, `copyback` `pbuf_take_at`, `prepend` `pbuf_add_header` (yer yoksa yeni pbuf + `pbuf_chain`), `cat` `pbuf_cat`, `adj` `pbuf_header/pbuf_realloc`, `pullup` `pbuf_coalesce`.
2. Per-opener sayım (`TnSocketBase` içinde `mbuf_outstanding`); `CloseLibrary`'de kaçak varsa `tn_logf` uyarı + serbest bırak.
3. `mbuf_abi_check.c` mantığı: `_Static_assert` ile `offsetof(struct mbuf, m_data)` vb. SDK değerlerine sabitle.
4. Host testi `tests/host/test_mbuf.c` (mock_lwip pbuf ile: zincir, copydata sınırları, prepend taşma, pullup, double free tespiti). Bench `tc_mbuf`.
5. `SBTC_HAVE_*`: mbuf API için kapasite tag'ı yoksa dokunma; `gen_lvo_table.py` 11 satır STUB→BUILT.
Kabul: host+bench yeşil, commit `lib: mbuf_* vectors over lwIP pbuf`.
