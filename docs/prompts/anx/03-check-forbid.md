# ANX-03 — `scripts/check-forbid.sh` statik kapı + `docs/FORBID.md`
Kurallar (değişmez): (1) yalnız bu dosyadaki iş; başka dosyaya, başka TNET'e dokunma. (2) Tek commit; iş bitmeden ara commit yok, iş bittikten sonra bekleyen değişiklik yok. (3) Kanıt = `ci/bench.sh` log dizini + host test çıktısı; kendi ifaden kanıt değildir. (4) DUR kuralı: bench üst üste 2 kez kırmızı → dur; 4 saat içinde commit yoksa → dur; durunca `STOP-REPORT.md` yaz (ne yapıldı, ne kırmızı, son yeşil commit) ve bitir. (5) Yeşil kalması gereken taban: mevcut core testleri iki profilde (a1200, 68000) — biri düşerse bu iş kırmızıdır. Kod davranışı değiştirme; yalnızca denetim ve belge.
1. `scripts/check-forbid.sh`: `src/` altındaki her `.c` için `Forbid()`/`Disable()` ile eşleşen `Permit()`/`Enable()` arasındaki bölgede şu çağrılardan biri varsa hata: `Wait, WaitPort, WaitIO, DoIO, Delay, ObtainSemaphore, AllocVec, AllocMem, OpenLibrary, OpenDevice, Open(, Lock(, Execute, PutStr, Printf, tn_logf`. Basit satır tabanlı tarayıcı yeter (Python, `scripts/`); iç içe nest sayısı tut; fonksiyon sınırı geçince sıfırla. Bilinen istisnalar `scripts/check-forbid-known.txt` (dosya:fonksiyon, gerekçe).
2. `src/lib/lib_vectors.c`'de `Forbid` altında `tn_logf` varsa istisna değil, bulgu: satırı `ISSUES.md`'ye `TNET-1xx OPEN` olarak yaz (düzeltme yok).
3. `Disable()` bölgelerinde ayrıca `Forbid()` dışındaki her şey yasak; SANA-II reply port'u için `Disable` şart (aygıt kendi kesmesinden `ReplyMsg` eder, `PutMsg` `mp_SigTask/mp_SigBit/mp_Flags`'ı tek `Disable` birimi olarak okur) — bunu `docs/FORBID.md`'ye tablo satırı olarak yaz.
4. `docs/FORBID.md`: her Forbid/Disable bölgesi için `dosya:satır | neden | ne bloklarsa bozar` tablosu (script çıktısından üret, elle yazma).
5. `Makefile` `python-checks` hedefine `check-forbid.sh` ekle.
Kabul: script çalışır, bulgular ISSUES'ta, commit `ci: add check-forbid gate and FORBID.md`.
