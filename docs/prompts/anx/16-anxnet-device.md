# ANX-16 — Ethernet sürücüsü: `anxnet.device`'ı **port etme, paketle**
Kurallar (değişmez): (1) yalnız bu dosyadaki iş; başka dosyaya, başka TNET'e dokunma. (2) Tek commit; iş bitmeden ara commit yok, iş bittikten sonra bekleyen değişiklik yok. (3) Kanıt = `ci/bench.sh` log dizini + host test çıktısı; kendi ifaden kanıt değildir. (4) DUR kuralı: bench üst üste 2 kez kırmızı → dur; 4 saat içinde commit yoksa → dur; durunca `STOP-REPORT.md` yaz (ne yapıldı, ne kırmızı, son yeşil commit) ve bitir. (5) Yeşil kalması gereken taban: mevcut core testleri iki profilde (a1200, 68000) — biri düşerse bu iş kırmızıdır. Referans: AmiNetXDuo `src/netdev/` (lance/dp8390/ne2000/el3, PCMCIA CIS, ISA-PnP, watchdog, multicast). Port yok; olduğu gibi paketlenir.
1. `vendor/anxnet/` = AmiNetXDuo `src/netdev/` + `src/sana2/` (sürücü tarafı) + `src/common/` bağımlılıkları; alınan dosya listesi `vendor/anxnet/FILES.txt`, üst commit hash'i `VERSION.txt`, MIT bildirimi ve `THIRD_PARTY_LICENSES.md` satırı. Değişiklik **sıfır**; gerekirse yamalar `vendor/anxnet/patches/*.patch` (upstream'e gönderilecek biçimde).
2. Derleme: AmiNetXDuo CMake'ini çağırma; `Makefile` hedefi `anxnet` yalnızca `src/netdev` kaynaklarını `-m68000 -msoft-float` ile derleyip `DEVS:Networks/anxnet.device` üretir (ThreadX/netstack include'ı gerekirse o dosya alınmaz; hangi dosyaların bağımsız derlendiği `FILES.txt`'e). Derlenemeyen kart yoksa dur ve `STOP-REPORT.md`'ye "anxnet bağımsız derlenmiyor: <hata>" yaz — bu durumda plan B: AmiNetXDuo release LhA'sındaki hazır `anxnet.device` ikilisini paketle, kaynak yerine `VERSION.txt`'e sürüm.
3. Wizard donanım sayfası (`src/setup/hw_detect.c`): tespit tablosuna anxnet'in desteklediği kartlar (Ariadne/A2065 lance, PCMCIA NE2000 uyumlular — CIS ile, 3Com el3) → aygıt önerisi `anxnet.device` unit N; mevcut `DEVS:Networks/*.device` listesi korunur, anxnet **ek seçenek**.
4. Bench: WinUAE `uaenet.device` kalır (anxnet gerçek donanım ister); `tc_driver_list` anxnet'in listede ve `OpenDevice` başarısız olunca (kart yok) wizard'ın düz mesaj verdiğini doğrular. Gerçek kart testi sahibi yapar → `docs/hardware-log.md`.
5. Installer: `anxnet.device` **yalnızca yoksa** `DEVS:Networks/`'e; varsa sürüm karşılaştır, yeni değilse dokunma.
Kabul: `make package` içinde `anxnet.device`, bench yeşil, commit `netdev: bundle AmiNetXDuo anxnet.device as optional SANA-II driver`.
