# ANX-06 — aminet-survey: gerçek yazılım hangi LVO'ları çağırıyor
Kurallar (değişmez): (1) yalnız bu dosyadaki iş; başka dosyaya, başka TNET'e dokunma. (2) Tek commit; iş bitmeden ara commit yok, iş bittikten sonra bekleyen değişiklik yok. (3) Kanıt = `ci/bench.sh` log dizini + host test çıktısı; kendi ifaden kanıt değildir. (4) DUR kuralı: bench üst üste 2 kez kırmızı → dur; 4 saat içinde commit yoksa → dur; durunca `STOP-REPORT.md` yaz (ne yapıldı, ne kırmızı, son yeşil commit) ve bitir. (5) Yeşil kalması gereken taban: mevcut core testleri iki profilde (a1200, 68000) — biri düşerse bu iş kırmızıdır. Ek kanıt: `docs/aminet-survey/*.tsv`.
1. `scripts/aminet-survey/callers.py`: girdi = bir dizin dolusu Aminet `.lha` (kullanıcı `E:\amiga\aminet-comm\` altına ~200 arşiv koyar: comm/tcp, comm/net, comm/www, comm/mail, util/libs AmiSSL). Her arşivi `lha x` (Linux `lhasa`), her Hunk yürütülebilir/kütüphaneyi tara: `bsdsocket.library` string'i yoksa atla; varsa kod hunk'larında `JSR -N(A6)` (`4EAE xxxx`) ve `JMP -N(A6)` (`4EEE xxxx`) kalıplarındaki 16-bit negatif ofsetleri topla; `-30…-858` aralığında ve 6'ya bölünenleri say. (Kaba ama AmiNetXDuo'nun yaptığı bu; `A6` yükleme bağlamı takip etme.) Çakışan ofsetler (başka kütüphane) için `lvo-collisions.tsv` üret: aynı ofseti kullanan ama `bsdsocket` string'i olmayan dosyalar.
2. Çıktılar `docs/aminet-survey/`: `lvo-usage.tsv` (offset, ad, kaç arşiv, örnek 3 dosya), `unused-vectors.tsv`, `results.tsv` (arşiv, tespit edilen vektör sayısı), `README.md` (yöntem, sınırlar).
3. `src/lib/lib_compat_table.gen.md` üretimine (ANX-02) sütun ekle: `used_by` sayısı; stub + kullanılan vektörler `docs/aminet-survey/worklist.txt`'e sıralı (çok kullanılandan aza).
4. `THIRD_PARTY_LICENSES.md`: "survey yöntemi AmiNetXDuo `tools/aminet-survey/callers.py`'den uyarlandı".
Kabul: en az 100 arşiv taranmış, `worklist.txt` var, commit `tools: aminet LVO usage survey`.
