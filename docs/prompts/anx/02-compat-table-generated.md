# ANX-02 — compat tablosu üretilsin, elle yazılan `docs/compat.md` kalksın
Kurallar (değişmez): (1) yalnız bu dosyadaki iş; başka dosyaya, başka TNET'e dokunma. (2) Tek commit; iş bitmeden ara commit yok, iş bittikten sonra bekleyen değişiklik yok. (3) Kanıt = `ci/bench.sh` log dizini + host test çıktısı; kendi ifaden kanıt değildir. (4) DUR kuralı: bench üst üste 2 kez kırmızı → dur; 4 saat içinde commit yoksa → dur; durunca `STOP-REPORT.md` yaz (ne yapıldı, ne kırmızı, son yeşil commit) ve bitir. (5) Yeşil kalması gereken taban: mevcut core testleri iki profilde (a1200, 68000) — biri düşerse bu iş kırmızıdır. Ek kanıt: `make python-checks` + bench log.
1. `scripts/gen_lvo_table.py`'ye ikinci çıktı: `src/lib/lib_compat_table.gen.md` artık üç kaynaktan üretilir: (a) SFD (offset, prototip), (b) `src/lib/lib_vectors.c` + `src/task/main.c` IPC `case` listesi → durum `BUILT` / `STUB`; daemon `default:` arm'a düşen marshaled çağrı varsa `BROKEN` (grep ile: LVO'da handler var, dispatch'te `case TN_OP_<x>` yok), (c) son bench dizinindeki `SUMMARY.txt`/TAP çıktısı → `PASS` sütunu (hangi `tc_*` testi kapsıyor).
2. `docs/compat.md`'yi sil; README'deki "61 implemented / 72 stub" sayılarını `gen_lvo_table.py`'nin bastığı sayıyla değiştir (README'ye elle sayı yazma: script `README.md` içindeki `<!-- lvo-stats -->…<!-- /lvo-stats -->` bloğunu günceller).
3. `make python-checks` içine kapı: üretilen tablo ile committed tablo farklıysa hata (AmiNetXDuo `check-vector-abi.sh` mantığı).
4. `TOLUNNET-COMPAT.md` §2'deki vektör listesini de üretilen tabloya link ile değiştir; elle liste kalmasın.
Kabul: `git grep -n "BROKEN (TNET-077)"` boş; `make python-checks` yeşil; commit `docs: generate compat table from source+bench, drop hand-written compat.md`.
