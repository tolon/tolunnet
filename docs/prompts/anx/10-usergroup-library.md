# ANX-10 — `usergroup.library` (AmiTCP/Roadshow uyumluluğu)
Kurallar (değişmez): (1) yalnız bu dosyadaki iş; başka dosyaya, başka TNET'e dokunma. (2) Tek commit; iş bitmeden ara commit yok, iş bittikten sonra bekleyen değişiklik yok. (3) Kanıt = `ci/bench.sh` log dizini + host test çıktısı; kendi ifaden kanıt değildir. (4) DUR kuralı: bench üst üste 2 kez kırmızı → dur; 4 saat içinde commit yoksa → dur; durunca `STOP-REPORT.md` yaz (ne yapıldı, ne kırmızı, son yeşil commit) ve bitir. (5) Yeşil kalması gereken taban: mevcut core testleri iki profilde (a1200, 68000) — biri düşerse bu iş kırmızıdır. Lisans: kaynak AmiNetXDuo `src/usergroup/` — her dosya başına dosya başlıkları korunur; `THIRD_PARTY_LICENSES.md` satırı.
1. `E:\amiga\AmiNetXDuo\src\usergroup\{ug_context,ug_db,ug_ids,ug_library,ug_misc,ug_parse,ug_str}.c` + `usergroup_internal.h`, `usergroup_vectors.h` → `src/usergroup/`. CMake yerine `Makefile` hedefi `LIBS/usergroup.library`; ThreadX/AmiNetXDuo'ya özgü include'ları (`ami_*`, `netstack_*`) `src/common/` karşılıklarıyla değiştir; değiştirdiğin her satırı `docs/usergroup-port.md`'de listele.
2. Veri kaynağı: `AmiTCP:db/passwd` ve `AmiTCP:db/group` varsa (salt okunur, `pr_WindowPtr=-1`, assign yoksa dokunma), yoksa `DEVS:Internet/passwd|group`, o da yoksa `DEVS:tolunnet/passwd|group` (Installer boş şablon yazar: `root`, `nobody`).
3. Vektörler: SFD `E:\amiga\Amigatolon\roadshow\Roadshow-SDK-1.8\sfd\usergroup_lib.sfd`'den `scripts/gen_lvo_table.py` ile üret (ikinci kütüphane desteği: `--sfd --prefix ug_`); uygulanmayanlar `ENOSYS` stub, tablo `src/usergroup/ug_compat_table.gen.md`.
4. Host testi: AmiNetXDuo `tests/bsdsocket/host/test_usergroup_host.c`'yi `tests/host/test_usergroup.c` olarak uyarla (`getpwnam`, `getpwuid`, `getgrnam`, `setpwent/getpwent`, `crypt` yoksa `ENOSYS`, `ug_SetupTagList`/`ug_GetSetup`, `ug_StrError`). Bench `tc_usergroup`: `OpenLibrary("usergroup.library",4)` + `getpwnam("root")`.
5. Installer: `LIBS:usergroup.library` **yalnızca yoksa** kopyala (Roadshow'unki varsa üzerine yazma; wizard "bırak" seçeneğiyle uyumlu).
Kabul: host test + bench yeşil, commit `usergroup: port usergroup.library from AmiNetXDuo`.
