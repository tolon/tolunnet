# ANX-04 — 68000 donması için sağlık sayaçları (`netstat -h`)
Kurallar (değişmez): (1) yalnız bu dosyadaki iş; başka dosyaya, başka TNET'e dokunma. (2) Tek commit; iş bitmeden ara commit yok, iş bittikten sonra bekleyen değişiklik yok. (3) Kanıt = `ci/bench.sh` log dizini + host test çıktısı; kendi ifaden kanıt değildir. (4) DUR kuralı: bench üst üste 2 kez kırmızı → dur; 4 saat içinde commit yoksa → dur; durunca `STOP-REPORT.md` yaz (ne yapıldı, ne kırmızı, son yeşil commit) ve bitir. (5) Yeşil kalması gereken taban: mevcut core testleri iki profilde (a1200, 68000) — biri düşerse bu iş kırmızıdır. Ek kanıt: bench log + `health.log`.
1. `src/task/` içinde `MEMF_PUBLIC` tek `struct TnHealth` (versiyonlu, `struct_size` ilk alan): `alloc_outstanding, alloc_peak, alloc_refused, sockets_open, sockets_peak, openers, pbuf_free, pbuf_total, pbuf_min_ever, pbuf_empty_hits, freed_twice, rx_posted, rx_completed, tx_posted, tx_completed, timer_ticks, last_ipc_op, last_ipc_tick, forbid_max_ticks`. Adresini `tolunnet.port` MsgPort'unun hemen ardındaki bilinen ofsete değil, `FindSemaphore("tolunnet.health")` ile bulunan `SignalSemaphore`'un `ss_Link.ln_Name` sonrası gövdesine koy (okuyucu semaforu **almaz**, sadece okur).
2. `netstat -h`: kütüphane açmadan, `AllocVec` yapmadan (statik tampon), `Forbid` almadan yapıyı okuyup basar; `AvailMem(MEMF_ANY)` da yanına. `netstat -h` daemon yokken "not running" der ve 5 döner.
3. `ci/bench.sh`: her cycle'da arka planda `Execute` betiği `Wait 5` döngüsüyle `netstat -h >>WORK:health.log`; log bench dizinine kopyalanır. Donma olursa son 10 blok `STOP-REPORT.md`'ye girer.
4. `rx_posted − rx_completed` ve `last_ipc_tick` ile `timer_ticks` farkı: donma anında hangisi durdu (SANA-II mi, timer mı, IPC mi) — `docs/bench.md`'ye okuma kılavuzu (AmiNetXDuo tablosu biçiminde: sayaç | anlam | kötü değer).
5. `ISSUES.md`: `TNET-113 | Blocker | 68000 nondeterministic freeze (c7c3c6d, 06b0aae) | OPEN` — bu prompt teşhis aracıdır, düzeltme değil.
Kabul: a1200 34/34 korunur; `health.log` bench dizininde; commit `diag: health counters + netstat -h for freeze triage (TNET-113)`.
