# ANX-13 — `ARCHIVE-MANIFEST` kapısı, `docs/GAPS.md`, README "Why" güncellemesi
Kurallar (değişmez): (1) yalnız bu dosyadaki iş; başka dosyaya, başka TNET'e dokunma. (2) Tek commit; iş bitmeden ara commit yok, iş bittikten sonra bekleyen değişiklik yok. (3) Kanıt = `ci/bench.sh` log dizini + host test çıktısı; kendi ifaden kanıt değildir. (4) DUR kuralı: bench üst üste 2 kez kırmızı → dur; 4 saat içinde commit yoksa → dur; durunca `STOP-REPORT.md` yaz (ne yapıldı, ne kırmızı, son yeşil commit) ve bitir. (5) Yeşil kalması gereken taban: mevcut core testleri iki profilde (a1200, 68000) — biri düşerse bu iş kırmızıdır. Ek kanıt: `make package` çıktısı.
1. `install/ARCHIVE-MANIFEST`: LhA'da olması gereken her dosya (yol, tür, `-m68000` ikili ise `objdump` 68020+ opcode taraması = 0 — TNET-074 yöntemi). `scripts/check_archive.py`: `make package` sonunda `lha l` çıktısını manifest ile karşılaştırır; eksik/fazla dosya → hata; ikili boyutları `docs/bench-logs/<son>/SIZES.txt`'e yazar (ADF sığma kontrolü: toplam < 880 KB - 5%).
2. `docs/GAPS.md`: eksik listesi, her satır `alan | durum | neden yok | plan/asla`. Başlangıç: Tier 2 stub grupları (routing/interface/monitor/DNS API'leri), `mbuf_*` (asla — lwIP pbuf), `ObtainRoadshowData` (asla), TLS (asla — AmiSSL), kendi sürücü (asla), IPv6 (ANX-11'e kadar), gerçek donanım kanıtı (yok — plan: A1200 + PCMCIA/X-Surf, sahibi koşar), micro build (ANX-12). `compat.gen.md`'den STUB listesi otomatik gelsin; el yazısı yalnızca "neden".
3. `README.md` "Why tolunnet?": "tek open-source stack" iddiasını kaldır; şu cümle: "AmiNetXDuo (MIT, NetX Duo tabanlı, bsdsocktest 142/142) aynı ihtiyacı kapatır; tolunnet'in farkı lwIP tabanıküçük bellek profili ve kurulum sihirbazı (rakip stack tespiti/devre dışı bırakma/geri alma)." bsdsocktest sonucumuzu (ANX-01 sayısı) yanına yaz, script bloğundan.
4. `CONTRIBUTING.md`: derleme, `make test-host`, `ci/bench.sh` ön koşulları (WinUAE yolu, `bsdsocket_emu=false`), commit biçimi `type(scope): summary`, "bench logu olmadan PR yok".
Kabul: `make package` manifest kapısıyla yeşil, commit `docs: GAPS.md, archive manifest gate, honest README`.
